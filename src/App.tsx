import React from "react";
import "./App.scss";
import {Counter} from "components";

const App = () => {
    return (
        <div className="app">
            <Counter />
        </div>
    );
};

export default App;
